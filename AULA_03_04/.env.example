# Provedor de embeddings: "huggingface" (padrão, local e gratuito) ou "openai" (paga)
EMBEDDING_PROVIDER=huggingface
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Só é necessário preencher se EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=sua_chave_de_api_aqui
